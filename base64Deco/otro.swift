//
//  otro.swift
//  base64Deco
//
//  Created by Enrique  on 24/08/16.
//  Copyright © 2016 Enrique . All rights reserved.
//

import Foundation
/*
func manageTxtFile(textOutput: String)
{
    let file = "file.txt"
    
    let text = textOutput
    
    if let dir = NSSearchPathForDirectoriesInDomains(NSSearchPathDirectory.DocumentDirectory, NSSearchPathDomainMask.AllDomainsMask, true).first
    {
        let path = NSURL(fileURLWithPath: dir).URLByAppendingPathComponent(file)
        print(path)
        //writing
        do
        {
            try text.writeToURL(path, atomically: false, encoding: NSUTF8StringEncoding)
        }
        catch
        {
            print(NSError)
        }
        
        //reading
        do {
            let text2 = try NSString(contentsOfURL: path, encoding: NSUTF8StringEncoding)
            tf_output.stringValue = text2 as String
            print(text2)
        }
        catch
        {
            print(NSError)
        }
        
                    //deleting
         do {
         try NSFileManager.defaultManager().removeItemAtURL(path)
         print("Clean")
         } catch
         {
         print(NSError)
         }
                }
}
*/

//        print("data2")
//        print(data2!)

//            let base64Decoded: NSString = NSString(data: data, encoding: NSUTF8StringEncoding)!
//        let base64Decoded = NSString(data: data, encoding: NSUTF8StringEncoding)
// NO, regresa a la cadena original
//        let date33 = data.base64EncodedStringWithOptions(NSDataBase64EncodingOptions.Encoding64CharacterLineLength)
//        print("date33")


/*        let base64Encoded = tf_input.stringValue
 let data: NSData = NSData(base64EncodedString: base64Encoded, options: NSDataBase64DecodingOptions(rawValue: 0))!
 print(data)
 */
//        tf_output.stringValue = String(data)
//        manageTxtFile(String(data3))

